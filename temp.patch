*** Begin Patch
*** Update File: app\(Dashboard)\dashboard\(admins)\(super)\superadmin\leads\(view)\view\LeadViewClient.tsx
@@
   const handleStatusChange = async (status: LeadStatus) => {
     if (!lead) return;
     setSelectedStatus(status);
     setShowStatusPopup(false);
@@
       toast.error(err?.message || 'Failed to update status.');
     }
   };
+
+  if (!leadId) {
+    return (
+      <div className="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
+        <p className="text-base text-slate-700">No lead ID provided.</p>
+      </div>
+    );
+  }
+
+  if (isLoading) {
+    return (
+      <div className="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
+        <p className="text-base text-slate-700">Loading lead details...</p>
+      </div>
+    );
+  }
+
+  if (error || !lead) {
+    return (
+      <div className="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
+        <p className="text-base text-slate-700">Unable to load lead details.</p>
+      </div>
+    );
+  }
*** End Patch
